# @burnerform/core

Canonical Burnerform runtime schemas, API contracts, and cryptographic
compatibility code.

The API error contract requires a stable `code`, public `message`, and safe
`retry` guidance in every error response.

Install it with:

```bash
pnpm add @burnerform/core
```
